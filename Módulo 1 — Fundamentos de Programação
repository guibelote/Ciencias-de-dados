#Módulo 1 — Fundamentos de Programação
# Função para calcular a média de notas
def calcular_media(notas):
  """Calcula a média de uma lista de notas."""
  if not notas:
    return 0
  return sum(notas) / len(notas)

# Solicitar as notas do usuário
notas_str = input("Digite as notas separadas por vírgula (ex: 7.5, 8, 9.2): ")

# Converter as notas para uma lista de floats
try:
  notas = [float(nota.strip()) for nota in notas_str.split(',')]
except ValueError:
  print("Entrada inválida. Por favor, digite apenas números separados por vírgula.")
  notas = []

# Calcular e exibir a média
media = calcular_media(notas)

if notas:
  print(f"A média das notas é: {media:.2f}")
